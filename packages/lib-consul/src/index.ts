import config from 'config'
import logger from '@gtms/lib-logger'
import Consul from 'consul'
import uuid from 'uuid'
import os from 'os'

export const registerInConsul = (serviceName: string, port: number) => new Promise((resolve, reject) => {

})
