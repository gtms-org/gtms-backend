export enum Queues {
  notifications = 'notifications',
  deleteAccount = 'deleteAccount',
}

export enum NotificationQueueMessageType {
  email = 'email',
  webPush = 'webPush',
}

export enum http {
  POST = 'POST',
  GET = 'GET',
  PUT = 'PUT',
  DELETE = 'DELETE',
}
